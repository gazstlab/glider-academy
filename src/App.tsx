// The landmark and nothing else. No copy lives here until S03 lands the
// locale loader — a string in a component is a §9 violation by construction.
export default function App() {
  return <main id="app" />;
}
